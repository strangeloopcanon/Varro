# LLMONLY_RUN (preserved artifacts)

This folder preserves LLM-only reward artifacts prior to composite-reward switch.

- training_data/: training/gspo_training_YYYYMMDD.json copies
- evaluations/: *_evaluations.json/csv from timestamped_storage_NEWRUN/
- checkpoints/: final_model (rolling) and latest dated snapshot if present
